<article id="photolist">
  <h1>Chesapeake 2013 Photos</h1>
  <p>Take a moment to view some of the photos from our last trip and get a sense of the terrain. </p>
  <div class="pixgrid">
  	<% if(photos.length > 0 ) { %>
  		<% for(i = 0; i < photos.length; i++ ) { %>
  		<img src="/images/photos/<%= photos[i] %>" alt="Photo <%= i %>">
  		<% } %>
  	<% } %>
  </div>

</article><!-- article -->